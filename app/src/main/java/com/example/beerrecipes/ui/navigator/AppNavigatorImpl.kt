package com.example.beerrecipes.ui.navigator

import android.content.Intent
import androidx.fragment.app.FragmentActivity
import com.example.beerrecipes.ui.ui.detail.DetailActivity
import javax.inject.Inject

abstract class AppNavigatorImpl : AppNavigator {

//    override fun navigateToDetailedBeer(beerId: Int) {
//        val intent = Intent(activity,DetailActivity::class.java)
//        intent.putExtra("beerid",beerId)
//        activity.startActivity(intent)
//    }

}